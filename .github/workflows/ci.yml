# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActionsSteps (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_ci --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: ci

on:
  push:
    branches:
      - main
      - iteration/*
    tags:
      - v*
  pull_request:
    branches:
      - main
      - iteration/*

jobs:
  Build:
    strategy:
      fail-fast: false
      matrix:
        os: [macOS-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          clean: 'false'
      - name: 📲 Install Nuke Global Tool
        run: |
          dotnet tool install -g Nuke.GlobalTool
      - name: 🎁 Restore
        run: |
          nuke Restore --skip
      - name: ⚙ Build
        run: |
          nuke Build --skip
      - name: 🚦 Test
        run: |
          nuke Test Trigger_Code_Coverage_Reports Generate_Code_Coverage_Report_Cobertura --skip
      - name: 📦 Pack
        run: |
          nuke Pack --skip
